DOcker Complete EXperiments

9. Pull a Docker image from Docker Hub and create a running container from it.

10. Create a Dockerfile for a sample application, build a Docker image from it, and create 
a container using that image.


firstly in bound rules add 8080 and 8081-9000 ports on custom tcp

login session without username 
then asks here type ec2-user

CMNDS:
sudo su -

vi /etc/hostname   keep it as docker admin

init 6

press R

#guve ec2-user

sudo yum install docker

service docker start

docker images

docker pull tomcat

docker run -d --name tomcat_container -p 8081:8080 tomcat:latest

docker images

docker ps

now copy ip address :8081 in browser

docker exec -it tomcat_container /bin/bash

ls

cd ..

cd webapps.dist

ls

cp -R * ../webapps/

cd ..
cd webapps
ls
exit
vi Dockerfile

FROM tomcat:latest
RUN cp -R /usr/local/tomcat/webapps.dist/* /usr/local/webapps/
:wq

foor building another image

docker build -t sample_image .

docker images

docker ps -a

docker run -d --name tc1 8082:8080

docker run -d --name tc1 -p 8082:8080 sample_image
refresh browser
and change port no 8082



